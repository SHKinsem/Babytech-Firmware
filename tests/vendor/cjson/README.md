# Host-test JSON dependency

cJSON v1.7.19, https://github.com/DaveGamble/cJSON/tree/v1.7.19.
Unmodified cJSON.c/cJSON.h and MIT LICENSE are vendored for offline host tests.
The ESP32 build uses the cJSON supplied by its pinned ESP-IDF SDK instead.
