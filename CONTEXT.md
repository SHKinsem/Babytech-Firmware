# 设备动作领域术语

本文统一设备板动作编排相关的术语；目标架构与实现进度见 [架构实施计划](docs/device-architecture-plan.md)。

## Language

**电机**：具有独立身份、机械参数和观测状态的实际执行设备。注册电机表示纳入管理，不表示使能或运动。

**动作程序**：描述设备动作顺序、参数和等待条件的一份可重复执行的定义。程序内容与某次运行的执行状态分离。
_Avoid_: 固件、运行实例

**程序运行**：动作程序某一版本的一次执行，具有独立的进度、结果和发起来源。

**指令定义**：程序中描述一次操作或等待条件的静态内容。

**指令实例**：某次运行中的一次具体操作，具有执行阶段、完成条件和结果。

**观测状态**：从硬件获得、具有有效性和采集时间的设备状态。期望位置或已发送命令不能替代观测。

**完成条件**：本次指令要求达到的完成程度，包括成功发出、协议确认或有效反馈证明动作完成。程序结束不必然表示所有电机停止。

**运动执行权**：当前允许发起设备运动的一次运行所持有的控制资格。释放执行权不等于证明硬件停止。

**同步运动**：多个电机按共同规划和触发约束执行的一次组合动作，不能等同于依次提交普通移动。
