# 开发与验证记录

本文保存阶段安排与历史验证结果，不替代 README 的使用说明。历史测试结论只对应当时版本，不表示当前功能已通过实机验收。

## 工程命名迁移（2026-09-23）

- `brain` → `main-controller`，`motion` → `device-controller`；旧构建参数兼容映射、配置及协议边界见 [迁移说明](controller-naming.md)。下方历史记录保留当时名称与产物路径。
- 新工程完整 WSL 双板编译通过：设备控制板 RAM 122508 bytes、Flash 1288237 bytes；主控板 RAM 45392 bytes、Flash 735797 bytes。产物写入新名称目录。
- Windows 与 WSL 主机协议、运动控制及真实 CAN 编码测试通过；网页 82 项测试、主控板浏览器测试及设备页 13 组检查通过，覆盖 1513×1039 和 1280×800。
- 设备网页在新路径重新构建，与迁移前内容一致；本次未烧录设备，未验证双 move 实机问题或同步动作。
- 旧 `-Target brain` 和 `-Target motion` 均已实际构建通过，分别导出到新主控板及设备控制板目录；文档本地链接检查通过。

## 阶段安排（2026-09-23）

1. 按 [电机修复与同步计划](motor-sync-plan.md) 排查双电机执行、改进异常呈现与 CAN 查询调度，完成同步及螺旋动作的分阶段实机验收，再接入机构动作 Runtime。
2. 实机验收已实现的 UART「改 RAM 参数 → 单电机阶段 → 返回结果 → 停止」闭环，见 [v2 指令表](protocol-v2.md)。
3. 将下板称重状态加入板间协议；下板调试页已接入称重、漂移诊断、去皮和标定。
4. 迁入已验证的屏幕/触摸驱动，在大脑上显示同一份小脑状态。
5. 根据真实调试反馈补齐开盖、关盖、混合与参数保存。

App、Cloud、多家庭权限、复杂恢复和全量测试平台不进入本阶段。
未来大脑负责页面和网络，小脑独占机械状态机与驱动；网络回调不直接操作电机。

## 验证记录（2026-09-21）

- UART v2 最小闭环：READ / WRITE / EXEC / STOP 已接通上下板，包括单电机阶段 RAM 参数、显式使能/失能、执行结果与优先停止。
- 21:20（香港时间）WSL 双板编译通过：motion Flash 1075561 bytes、RAM 72368 bytes；brain Flash 735717 bytes、RAM 45392 bytes。motion 构建已包含 HX711、NVS 标定和称重 API；产物位于 `out/wsl/motion/` 与 `out/wsl/brain/`，两板须一起更新到 v2。
- 协议测试：v1 回归及 v2 分段/粘包、CRC、批量写原子性、重复请求去重、丢回复结果补查、断线停止、重启恢复和查询限速通过。
- MotionCore 133 项检查、称重处理器 3 组场景、电机控制器 679 项检查，以及 UART 执行入口与真实控制器的模拟 CAN 联调通过；使能/失能缺 ACK、停止未确认不会报告成功。
- 上板嵌入页面 Playwright 测试通过，覆盖无自动动作、参数版本、未应用编辑、异步结果、停止、离线及重启。下板工作台的既有验证记录见其交付文档。
- 未进行硬件烧录、实际 UART 接线验收或浏览器实机测试。
