# 固件整合进度与协作步骤（2026-09-24）

整合分支：`codex/firmware-integration-20260924`。用户已要求将通过软件检查的整合作为主线协作基线，优先让协作者继续调试 sync；合并状态见 [PR #12](https://github.com/SHKinsem/Babytech-Firmware/pull/12)。本轮未烧录设备或完成整合镜像的实机验收，剩余验证继续在 issue 中跟进，不再以所有外围功能验收完成作为上主线的前提。

## 最高优先级：保住昨晚验证过的 sync

用户于 2026-09-24 明确：sync 优先级最高，尤其是昨晚验证出来的代码。其他 UI、OTA、demo 和目录清理均应服从这一基线；若整合引入 sync 回归，应拆出或修正相关整合改动，不以旧 main 的实现覆盖已验证行为。

- 实机证据见 [同步开发记录](motion-sync-development.md#2026-09-23-同步实机补充)：X42S 缓存阶段目标保持旧值，须在 FF 后通过新查询确认目标；相对目标使用显式 0.5° 窗口。双轴反向三圈的最终记录镜像 SHA-256 为 `a9a5fa23145293954b3e350ab87cae010e3c841d565ae5996ab6094a2dead9c3`，981/981 查询响应、两轴完成，末尾三台均失能静止无故障。该记录有明确工况边界，不代表全部同步精度或负载验收完成。
- 当前整合保留 `d032657`（X42S 触发后目标确认）、`d176088`（trigger-only 模式）、`3e966be`（移除人工许可门槛）、`e9e256a`（活动反馈轮询与 await 目标校验）。以上提交均为整合分支祖先；不能把“保留提交”误当成整合镜像已实测。
- 与 PR #7 的 `dafbf81` 对比，改名后的 `CanQueryScheduler.h`、`SyncPlanner.h`、`SyncRuntime.h` Git blob 完全相同。外围 `MotorControl`、`CommandQueue` 和主循环已有整合修改，仍需优先复查及复验；不宣称最终镜像与昨晚镜像等同。历史记录未直接绑定最终实机镜像到唯一提交，后续验收需补充提交 SHA、构建配置和镜像哈希三者对应关系。
- 下一步先复验昨晚相同的 sync 程序、参数与反馈语义，再验收其他功能。首先保护触发后目标确认、查询预算、目标窗口、停止覆盖以及真实完成判定；不得为整合方便静默改变这些策略。
## 已完成

- 以远端 main `d0435a7` 为基线，保留合并历史，整合 #1/#7 同步运动、#9 命令反馈、#5 紧凑界面、#8 OTA，以及 #10 整合计划。
- 修复 #9 批量温度 0x43 的正负号；保留 0x39 不同的符号约定。修复 #5 的 `60.` 等非法输入绕过人类单位换算的问题。
- 部分处理 #6：队列接管保留真实观测；F3 必须由真实 ACK 和后续状态确认；有效 disable 可中止 UART/队列/demo 所有权后实际发送；正常运行不再误报 Stopping。
- 统一目录为 `main-controller/`、`device-controller/`。PlatformIO 环境与 OTA 板型标识仍为 `brain`、`motion`，详见 [命名约定](controller-naming.md)。构建、打包、测试及文档路径已配套更新。
- 新增 GitHub Actions，执行主机回归、前端构建、主控固件和设备控制器两种 UART 配置编译。

## 本地验收

协议、运动、raw CAN、demo 主机测试通过；前端单元测试 92/92、Sites 测试 4/4、OTA 页面测试 3/3、刷写工具模拟测试 8/8 通过。设备页面和 Sites 构建通过。原生 PlatformIO 6.1.19 / espressif32 6.4.0 编译主控、设备 BRAIN 与 DISPLAY 配置通过。WSL 脚本通过语法检查，但本轮未在 WSL 实际编译。

浏览器 QA：设备页面 14/14 通过（包含 `60.` 禁发与 `60` 编码为 600 的回归），同步页面两种视口、显式设置保存、停止锁及无自动 POST 检查通过，命令反馈页面 QA 通过。全部 API 使用 mock，未接触设备。最终 DISPLAY 固件已核对精确包含提交的 HTML。整合记录见 [PR #12](https://github.com/SHKinsem/Babytech-Firmware/pull/12)。以上软件检查不能代替 UART/CAN 电机实测、掉线停止测试或签名 OTA/回滚验收。

## 协作者接下来怎么做

1. 审阅整合 PR，优先检查跨入口 disable、队列接管、同步反馈与 OTA maintenance 互斥。基于整合分支继续开发，避免再次把旧主目录整体覆盖到 main。
2. #6 保持开放：明确离线节点影响范围、0.5 与 0.2 RPM 停止阈值、到位容差和反馈超时依据；增加对应回归，并按真实电机反馈完成验收。尚无指定负责人，认领时在 issue 中明确范围与验收证据。
3. #3 / #8 保持待验收：核对设备已安装的 OTA 板型标识、签名公钥、维护状态互斥、健康确认和回滚。未通过前不要把“编译成功”当作可部署结论。
4. 完成主控—设备及 DISPLAY—设备两种接线的启动、使能、单轴/同步/队列/demo、停止、disable、断线恢复测试，记录固件提交、配置和实际结果。#4 mDNS 不在本轮实现范围。
5. 整合 PR 合并后再核对并关闭已覆盖的旧 PR #1/#5/#7/#8/#9/#10；不要提前关闭尚未满足验收条件的 issue。main 更新后重新检查冲突与 CI。

新出现的 `codex/bench-demo-config` 是独立协作分支，本轮未合并、修改或清理，后续单独评估依赖与行为重叠。

## 本地保护与清理记录

原主目录全部工作已保存到 `codex/preserve-local-main-20260924`，提交 `3f27a9f`；旧 main 提交也仍保留；保留分支已推送到远端。32 个文件的 SHA-256 备份、补丁和验证通过的 `repository.bundle` 位于本机 `out/integration-backup-20260924/`。根目录临时脚本与两张截图已归档到其中的 `root-artifacts/`。这些本机文件不随 Git 同步，协作者应以保留分支和整合 PR 为准。

本轮创建的单位修复和温度修复两个临时 worktree，已确认干净且其提交均为整合分支祖先后回收。原主目录保持干净，停留在保留分支；最终整合位于本机 `out/integration-worktree/`。

没有批量删除 `out/`、第三方参考工程或签名私钥。私钥未读取、移动或提交。既有 PR 工作区应在整合审阅后按“确认干净、保留分支、确认无活跃任务”的顺序回收。

[原始整合计划与审计快照](integration-plan.md) 保留最初观察；当前进度以本文和 GitHub PR/issue 的实时状态为准。
