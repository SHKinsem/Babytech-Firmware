# 测试运行器待修复项

## Windows 断言失败弹出应用程序错误窗口

- 记录日期：2026-09-24。
- 状态：待修复；用户要求先记录，本次不修改测试实现。
- 现象：运行 `python tools/test_demo.py` 时，`demo-motor.exe` 弹出 Windows「应用程序错误」窗口，提示 `unknown software exception (0x40000015)`，需要点击「确定」。用户提供了现场截图。
- 上下文：为 demo 后台查询导致 `move await` 卡住的问题建立失败回归时出现。对应日志包含 `targetRead=0 homeSent=0`、`waiting_position`，随后触发 `tests/test_demo_motor.cpp` 中的断言，Python 报告子进程退出码 3。
- 证据边界：已确认测试断言失败和弹窗同时发生；异常码的具体运行库处理机制尚未单独验证。该弹窗属于本机测试程序，不能据此判断 ESP32 固件崩溃。
- 影响：失败测试打断用户操作，可能阻碍无人值守运行。业务修复后测试通过，并不表示测试运行器的失败路径已经修好。

### 后续修复与验收

1. 用最小失败断言复现 Windows 弹窗，检查测试运行器及 C/C++ 运行库的错误报告方式。
2. 让失败结果写入标准错误并以非零状态退出，不弹出交互窗口；保留用例、断言和源码位置，不能吞掉错误或把失败改成成功。
3. 分别验证正常通过、断言失败、异常退出和超时，确保 Python 能结束子进程并清理临时文件。
4. 检查其他原生测试可执行文件是否存在同样问题；不改变固件业务逻辑。

本机原始失败日志：`out/queue-home-repro/red.log`（未纳入 Git）。
