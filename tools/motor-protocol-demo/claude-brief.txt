Implement the Babytech motor protocol workbench desktop preview demo in this folder using the existing React/Vite starter. You are Claude Code, explicitly selected by the user to develop this demo. You own implementation files only within this demo directory. Other work may exist in the repository: do not change or revert anything outside this directory. No git operations, deployments, hardware connections or real API calls. Do not start a server. Read references/design.png first; it is the exact selected design target. Read the copied firmware sources in references for byte-level grounding. Build the actual files, do not only describe a plan.

Deliver an excellent finished Chinese desktop frontend matching the image's white/indigo UI, three-column instruction lab and bottom CAN trace. Use actual text/components, never the reference as a screenshot background. Use system Chinese fonts (Microsoft YaHei UI/PingFang SC), compact precise typography, thin separators, minimal borders, no large hero text. Desktop target 1513 x 1039 with graceful usable 1280x800 layout (trace remains accessible). No external fonts/assets/network requirements. There are no raster asset slots. Icons: use @phosphor-icons/react if installed; do not hand draw SVG or use emoji icons.

Core functionality:
- Top toolbar CAN ID validated integer 1..255, connection toggle SIMULATED ONLY labelled 模拟模式, fixed 全部停止. Bitrate display 500 kbit/s. Changing motor IDs should keep independent per-motor simulation state or reset clearly, not silently transfer state.
- Main tabs 常规试动 and 指令实验室 (default). Main instruction lab left searchable categorized command library; middle dynamically generated typed parameter forms; right motor position/speed/current/enable state and latest response; bottom scrollable TX/RX log with filters, pause display and clear.
- Catalog exposes EVERY public motor command in references/X42sProtocol.h (excluding CAN infrastructure functions): zero current pos, reset clog, all readSysParams options, ctrl mode, realtime position interval, enable, torque + speed limit, velocity + current limit, passthrough position + current limit, trapezoid position + current limit, stop, sync trigger, origin zero/config/trigger/interrupt. Group them clearly. Show function code on each item. Fields have units, sensible defaults, ranges; actual physical parameter meanings only when established by source. Raw ambiguous firmware fields labelled protocol fields rather than guessing units/mode meanings. Torque is current-controlled mA, do not invent Nm. Preserve safe validation: integers for raw byte/word params, big endian, no silently clamped overflow, finite number validation, motor address in CAN identifier not payload. Use generated fields if helpful. Catalog search by Chinese name, hex opcode, English terms. Distinguish driver-supported vs web-integrated; prototype simulates all but do not claim firmware HTTP API supports all. Right capability information compact rather than a large marketing panel.
- Parameter editor and raw HEX tab. Actual editable logical command bytes (address + opcode + params + 6B). Invalid hex, missing required bytes, out-of-range address etc disable send with useful inline reason. Custom command mode supports logical command construction, clearly validates structure, and generates CAN frames; unknown raw commands only return TX plus 'no decoder / simulation unavailable', never fabricate successful effect.
- Logical command preview and actual extended CAN frames. Faithfully implement sendCommand splitting: remove address and opcode, split remaining bytes in groups of 7, prefix opcode for every CAN frame, ID=(address<<8)|packetIndex. Show each ID, DLC, payload, packet index. Copy logical command or frame rows works with status toast.
- Initial F3 enable default example logical 01 F3 AB 01 00 6B; frame ID 0x00000100 DLC5 DATA F3 AB 01 00 6B. Initial position180 speed0 current210 enabled true, visibly sample data. Trace seeded with sample F3 TX, F3 02 6B RX and position feedback 36 00 00 00 07 08 6B. Every record labelled simulated through panel context.
- Sending supported commands appends correct TX frames and simulated RX where known. Distinguish queued/received/feedback completion; avoid claiming ack means mechanical completion. Bounded motion simulation for normal relative move; current position updates, velocity/current visible and stop cancels outstanding timers so no late movement/completion. Enabling/disable state functional. Motion requires enabled; global stop remains usable anytime. Advanced origin/config commands show simulation-only explanatory result without inventing physical homing. Sync option queues without movement; FF executes pending simulation where supported. Don't let a stale async task finish after stop, motor change or new command. Real command variants can have frame preview even where simulation effects aren't modelled. No real hardware requests anywhere.
- 常规试动 reuses correct CD packet generation. Relative angle, direction, speed, accel/decel, current limit, predicted target, enabled gate and same simulated motor state. This tab should recall the earlier clean left feedback / right actions layout; no need for elaborate plotting library. A modest live numeric feedback is fine, don't write custom SVG.
- Surface validation in UI, disabled controls for incompatible states, keyboard focus visibility, consistent hover/selected states, accessible labels. Log filters/copy/search/category expansion/tab navigation should all work. Pausing the trace pauses its display rather than dropping underlying records. Bounded log memory.

Implementation: create src/protocol.js (pure catalog and packet encoding/validation), src/App.jsx, src/styles.css and supporting files as useful. Add meaningful node:test protocol tests (F3 example, CD split, all command encoders structure, invalid ranges, raw bytes, zero reads, max packet if relevant). Update package.json test script to run them. README.md Chinese with npm install, npm run dev -- --host 127.0.0.1 --port 4175, and limits. Use preinstalled template build scripts. Run npm run build and npm test if permissions allow; otherwise report. You can use Read Write Edit Glob Grep and npm run build/npm test. No need for subagents. Do not ask questions: use these concrete specs and implement fully. At end give concise summary of files and test results.
