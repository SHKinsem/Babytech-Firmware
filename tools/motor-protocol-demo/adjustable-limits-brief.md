# Implement configurable board motion limits

Use existing Claude Code / DeepSeek session and implement now. Other concurrent edits exist, especially scale pin configuration: preserve them. Own ONLY src/DeviceApp.jsx, src/device-api.js, src/device.css, src/components/ManualPanel.jsx, src/simulation.js, new src/components/LimitsPanel.jsx, optional new src/device-limits.js. Codex owns all backend, protocol.js, tests, QA, docs, builds. Do not edit ownership outside these files. File-only tools; Codex runs npm and browser. No hardware/network/git. Desktop only current white/indigo visual.

User is unhappy hidden fixed limits reject velocity command acceleration1000 (screenshot F60003E803E8006B). Must expose adjustable persistent settings and keep form/raw/manual matching actual board policy. Do not enable unsupported sync/homing/FD paths.

API contract Codex implements:
GET /api/limits -> {maxSpeedRpm:120,maxAccelRpmS:240,maxCurrentMa:5000,maxAngleDeg:3600,maxMoveSeconds:60,experimentSeconds:5} (these are INITIAL defaults only).
POST /api/limits form-urlencoded requires ALL six fields -> same complete object after NVS save succeeds. Server400 bad values,409 limits_busy if active/pending motor operation or UART operation,500 limits_save_failed. Save must not send device-controller/enable. Fields physical units, speed/angle allow0.1 steps; other fields integers.
Bounds: maxSpeedRpm0.1..3000 (decimal documented3000RPM despite typohex);maxAccelRpmS1..65535;maxCurrentMa100..5000;maxAngleDeg0.1..360000;maxMoveSeconds1..3600;experimentSeconds0..3600 (0=disable timer, explicit label; feedback/fault protection still works). These are implementation/wire ceilings, editable limits are board policy. Display distinction concisely. NVS persisted reboot; applies global Motion webdebug currentsettings not per-address.

Implement dedicated '调试限制' tab with compact editable fields, current loaded values, explicit 保存到板上 button, loading/offline/error state. No automaticPOST, no save per keystroke, no optimistic successful state; after POST adopt response, refetch on reenter/reconnect. Reset fields to defaults is local only until save. Shared confirmed limits state in DeviceApp, passed to LimitsPanel; GET initial and poll(refresh e.g2s) so other browser changes propagate without overwriting dirty form drafts. On missingGET/invalid payload keep operation gate disabled for motion, no fallback pretend loaded120/240. Fake API tests Codex willupdate. Preserve scale/Wi-Fi.

Use confirmed limits in supportReason(bytes,limits) F6/C6 acceleration1..maxAccel, velocity<=maxSpeed*10, current(C6)100..maxCurrent;F5/C5current0..maxCurrent, C5speed<=maxSpeed*10. CD mode2 remains only; numeric bounds & plan validated using samelimits; speed/angle/current/accel checks on raw editor too, clear specific reason. Otherread/nonmotion remain available when limits unavailable.
Defaults for velocity acc0 currently invalid; use device default60 in choose/variant when itemvelocity (and clamp current default to confirmed max if lower). Don't edit globalcatalog or silently change user-entered values when limitschange. Show configured limits in visible gate/help. Static field catalog supports protocol max but device gate explains board's configurablepolicy. ManualPanel accepts limits prop mapped MANUAL_LIMITS (default retains offlineAppbehavior). planManualMove(values,limits=MANUAL_LIMITS) all references use parameter. DeviceApp converts confirmed config to manual limits maxSpeedRpm,maxAccelRpmS,maxCurrentMa,maxAbsAngleDeg,maxExpectedDurationMs; min values unchanged; no browser-specific global mutation.

Replace fixed '5秒' labels with configured experimentSeconds,0 means '持续运行，手动停止'. Backend command result now message='queued_experiment' instead queued_auto_stop_5s; legacy message support okay. stateLabels experiment_running='试验运行中'. Existing unsupportedoperation message must not falselysay5sec.

Fix reporting defect: trace-gap notice separate traceWarning state and render near trace; NEVER overwrite request notice. HTTP error response gives concrete rejection; do NOT append '请求超时不代表未执行' to every HTTP error. request() may attach error.status; only network/timeout include uncertainty. Fixed 'HTTP202' explanation should read generalmeaning without suggesting failedrequestqueued.

Acceptance: acc1000 blocked under confirmed240 with specifichint, allowed once saved2000; speed100RPM encoded1000not100; setting0duration explicitcontinuous; dirty form unaffectedpoll, failedsave keeps actuallimits, noautoPOST; disconnectedlimits preventsmotion; F3stop/readavailable; manualplannerallows changedlimits, raw respects them; tracegapcannotoverwrite400error; all current tests preserved. Return bounded summary withpaths. Codex tests, builds and flashes separately.
