# Implement queue UI through Claude Code/DeepSeek

Read AGENTS.md and queue-contract.md; authoritative API/DSL contract there. Other work is happening. Ownership ONLY src/DeviceApp.jsx, src/device-api.js, src/device.css, new src/components/QueuePanel.jsx, new src/queue-program.js, new tests/queue-program.test.mjs, new qa-queue.mjs; package.json only append new test to existing command preserving other tests. Do not edit backend, other components or offline App. Implement, not just plan. Codex runs commands/browser tests; file tools only.

New device tab 编排队列 with source editor and optional concise action-builder, target ID, per-ID rotationDistance mm/rev saved via API, units deg/rev/mm, move/home/torque/velocity/wait/enable/disable/stop/raw logical/raw CAN. Persist draft in localStorage (no automatic start), repeat count, sample matching user motor1 move/motor2 home/motor3 torque with explicit enable lines, import/export/copy. Program remains easy to hand-edit; help lists short forms/defaults. Visible no Nm, per-ID distance mandatory for mm. Validate pure parser syntax and numeric framing; server does full policy validation. Preview readable summary; include source-line numbers.

Read queue-contract.md precisely for HTTP fields/DSL and outcome meaning. Poll queue status while device page mounted, including away from queue tab if needed for conflict gating. Queue busy disables manual device-controller/config/limits interactions; backend authoritative; stop always available. Running state not optimistic done; missing network hides success and exposes unknown execution. Start POST once, no automatic retry; cancellation always available. Draft edits/target/profile fetch races must not overwrite user's draft, wrong ID profile save not adopted. GET failed profile never imply default; 0 clear explicit. Error shows HTTP rejection separately from uncertain delivery. QueuePanel can own API+poll but share onQueueBusy callback with DeviceApp; keep imports simple.

Homing frontend fix now: remove9A from preview-only blacklist; isMotionOpcode include9A; gate mode<=5 sync0; stateLabels homing/ home result forthcoming from backend status {homeOutcome:none|running|done|already_at_origin|cancelled|failed, homeMode, homeFlags}. Fix old blanket '回零仅预览' and hardcodedtrialwarning. Raw existing lab path still protected; direct raw available in queue explicit hex/can. 4C dynamic speed(maxSpeedRpm) and current policy, timeout fullu32; existing poweron remains unsupported in supervised lab, explain raw path if useful.

Tests pure syntax including units/ranges/unknown verbs/raw IDs/DLC, malformed numeric/overflow; browser mockedAPIs (not actual motors) start exactly once, failedstart/noautoresume, polling progress, cancel, distinct-ID rotation distances+savefailed behavior,1280x800&1513x1039 nooverflow. Preserve existing regression behavior. Return files and checks not run.
