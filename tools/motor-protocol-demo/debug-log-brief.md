# Implement diagnostic logs now

User: “能不能再加个log，md每次debug都不知道什么问题”. They want practical diagnostics, not just transient CAN frames. Continue Claude Code / DeepSeek workflow. You are not alone: preserve ALL existing dirty changes (including recent queue-config recovery and drafts). File tools only; Codex runs checks. No flashing, network hardware, commits or changes to motion policy.

Own new device-controller/src/DebugLog.h (small fixed-capacity logger), narrow integration device-controller/src/main.cpp, new frontend src/debug-log.js and components/DebugLogPanel.jsx, src/device-api.js request instrumentation, src/DeviceApp.jsx log tab + ingestion, additive src/device.css, tests/debug-log.test.mjs, qa-debug-log.mjs, package.json append test. Optional native tests/test_debug_log.cpp and narrow native test runner addition. DO NOT modify MotorControl, CommandQueue, WiFiSetup or other control behavior. Read existing source/AGENTS first.

## Board contract

- GET /api/logs returns {bootId:string, uptimeMs:number, sequence:number, capacity:number, events:[{seq,atMs,level,event,detail}]}. Use existing setup boot random to initialize unique hex bootId, reset reason esp_reset_reason logged once. Fixed ring 48 or64 entries in static/global storage, detail bounded ~240 chars, no giant stack locals, no serial logging loop or writes to flash. JSON escaping correct for quotes/backslashes/control chars; detail truncation explicit. Main loop thread only (no WiFi callback mutation).
- Startup event + CAN ready + HTTP ready. Poll cheap scalar state changes each loop (not statusJson allocations each ms): motor.ready, faultTag, moveOutcome, homeOutcome/homeId, configMessage, queue state/runId/message/errorLine, WiFi.status. Emit transitions only. Don't claim UART/raw actions observed before/after polling are a complete audit or complete every queue step. Do not store passwords/SSID or raw WiFi payloads.
- sendJson path can log POST response for allowlisted motor/queue/limits/scale routes, status and bounded response. Include sanitized allowlisted request values (id/hex/angle/speed/current etc, never enumerate arbitrary args; no WiFi credentials). Response summary never log full arbitrary bodies or unknown path/query strings. Queue program itself captured by browser. GET logs must not log itself. Do not add timeouts/retries/blocking to loop, change responses, or send any motor frames. Check existing sendError/sendResult/sendQueueResult all route sendJson before selecting hook. Native logger test JSON escaping/ring chronological order/bounds/boot identity if possible with tests/fakes Arduino.

## Browser contract

- New desktop tab “调试日志” in existing layout. Maintain existing bottom CAN trace; main new panel offers level/source/text filter, copy, export Markdown, export JSON, clear (local history only). Readable time, severity, category, reason, expandable details, request ID. Compact Chinese hints: request submitted / HTTP202 queued / actual ACK / completed distinct. Never infer causal CAN matching merely by timing. Export includes current confirmed limits, snapshot selected motor, queue/config-result when observed, retained CAN frames, log version/time and origin WITHOUT URL query or credentials. Can export while offline.
- Pure singleton or store with subscribe for logs, safely isolated from request behavior. request() emits one start/result/error per POST with local requestId, sanitized inputs, exact HTTP status/error code/body useful fields, duration, uncertain flag. GET polling: deduplicate failure/recovery, record changes for status (state/fault/enable/canReady/lastAck, not each changing current) / queue state+run+step+message / config-result transaction changes / limits confirmed. Never recursive calls from logger. Ignore intentional abort request_cancelled cleanup. /api/logs GET excluded from generic GET failure logging to avoid 404 storm. Sensitive input redaction via allowlists, including nested response values (WiFi password never saved). Logger cannot break successful requests even storage denied, subscriber throwing etc. Maintain current errors/status/uncertain API contracts.
- Board logs poll every ~2s regardless active tab; 404 old firmware visible once and slower poll or stop; never labels missing endpoint device offline. Dedup by bootId+seq; detect restarts and ring gaps. Reconnect retrieves current ring; unknown older gap explicitly shown, board ring lost on power reset. No misleading full historical claims.
- Browser event history sessionStorage or localStorage versioned/bounded ~500 events, save debounced, survive reload; failure safe memory fallback. Keep CAN frames separately bounded e.g last400 in export and live log store, don't flood event list with periodic status query frames. Ingest existing accepted trace rows instead of extra /api/trace polling; record gaps/restart in events. Preserve previous-session history as historical, never restore live board state from logs. Do not record form edits as actual sends. Limit data sizes/capacity; clear doesn't erase motor drafts and doesn't immediately reimport board history (keep cursor).
- Add honest hint: board events RAM only / not full serial console, browser history local origin; record only observed CAN and gaps; refreshed historical state never drives control.
- No unauthorized autoflash/autosend; GET logs readonly. Preserve drafts, all config result UI/recovery, queue epochs, existing controls and POST flow.

## Checks to author (Codex runs)

- Pure tests for bounded ring/persistence corrupt+denied, request redaction nested/password/unknownfields, exports escaping, board dedup/reset/gap, GET unchanged polling not flood. Ensure logger cannot affect requests and exported secrets absent.
- Playwright following qa-current-limit.mjs conventions (real Chrome mocked device.test, built device-controller/data/index.html): 1513x1039 +1280x800; send 45 gets HTTP409 explanatory reason captured, another request 202 explicitly queued, timeout uncertain no automatic retry; fault/queue/config changes logged only once; board endpoint ring reconnect/reset/gap; old404 gracefully; reload log history still present but no automaticPOST; clear and export MD/JSON include evidence/no wifi password; no console errors. Don't test timeout with brittle short waits; use delayed route response beyond1800ms.
- Report owned changes, real limitations, no unrun pass claims. Keep new logger implementation compact and focus end-to-end usability. Finish edits before final report.
