# Babytech 电机协议工作台（桌面原型）

已增加独立实机入口 `src/DeviceApp.jsx`，集成 Wi-Fi 与真实板端 API。`npm run build:device` 生成固件内嵌网页；本地访问 `/?device` 可以检查实机界面（需要同源板端 API 才有数据）。默认 `/` 仍是原来的仿真 Demo。完整交付范围见 [固件交付说明](../../docs/motion-workbench-release.md)。

X42S / X28S 电机 CAN 协议的桌面端调试前端原型：左侧指令库、中间参数编辑器、右侧电机反馈、底部 TX/RX 收发记录。
原型的字节布局与校验参考 `references/` 中的固件源码。默认 Demo 为本地模拟；实机入口则向当前设备提交 HTTP 请求，并由当前固件再次校验。

## 运行

```bash
npm install
npm run dev -- --host 127.0.0.1 --port 4175
```

其他脚本：

```bash
npm run build      # vite build + scripts/prepare-sites-build.mjs
npm test           # node:test 协议单元测试（tests/protocol.test.mjs）
npm run test:sites # Sites 打包自检（需要先执行 npm run build）
```

目标分辨率 1513×1039；在 1280×800 下三栏收窄、中部面板内部滚动，底部收发记录始终可见。
字体只使用系统中文字体（Microsoft YaHei UI / PingFang SC 等）；图标使用已安装的 `@phosphor-icons/react`（逐图标子路径导入），无外部字体、图片或网络请求。
参数字段的详细说明收在控件旁的提示气泡里（悬停或聚焦可见），表单本身保持紧凑。

## 目录

| 文件 | 作用 |
| --- | --- |
| `src/protocol.js` | 纯协议层：指令目录、字节编码/解码、`sendCommand` 分帧、校验、搜索 |
| `src/simulation.js` | 纯仿真层：反馈帧编解码、ACK 状态、梯形时长、指令效果规划、常规试动校验 |
| `src/App.jsx` | 状态编排：电机模拟状态机、定时器/纪元管理、收发记录、复制与提示 |
| `src/components/` | 工具栏、指令库、指令面板、反馈面板、常规试动、收发记录、帧列表、字形 |
| `src/styles.css` | 全部样式（白底 + 靛蓝主色、细分割线、紧凑排版） |
| `tests/protocol.test.mjs` | node:test 协议测试 |

## 协议依据

| 来源 | 用途 |
| --- | --- |
| `references/X42sProtocol.cpp/.h` | 每条指令的字节布局、`sendCommand` 的 7 字节分包、`constrainCurrentMa` 上限 |
| `references/x42s_can_id.h` | 扩展帧 ID = (地址 << 8) \| 包序号，地址不放在数据段 |
| `references/MotionCore.h` | 线上单位（位置 0.1°、速度 0.1 RPM、电流 mA、加减速整数 RPM/s）、ACK 状态字节、梯形时长算法、请求取值范围 |

界面上每个参数字段都带标注：

- **源码明确**：单位与含义在固件源码中直接可查（例如位置 0.1°、速度 0.1 RPM、电流 mA、方向 0/1）。
- **参数名推断**：源码只给出参数名（例如 `intervalMs`、`stallMa`、`accelMaPerSec`）。
- **协议字段**：固件只暴露原始字节，含义或单位未在源码中定义（例如 `motionMode`、`ctrlMode`、`clk`、`maxSpeed`），界面不做猜测，按原始字节透传。

力矩类指令按**电流（mA）**下发，界面不使用 Nm。

## 功能范围

- 顶部工具栏：CAN ID（十进制 1..255，写入扩展帧 ID 高字节）、位速率显示（500 kbit/s / 1 Mbit/s，仅显示）、**模拟模式**连接开关、固定的「全部停止」。CAN ID 非法时两个标签页都会禁用发送并给出原因。
- 全部停止走**广播路径**：发送扩展帧 ID `0x00000000`、数据 `FE 98 00 6B`，同时作废所有地址的模拟运动、定时任务与待触发队列；广播没有寻址应答方，因此不会伪造 ACK。
- 指令实验室（默认）：左侧可搜索、可折叠的分组指令库；中间按指令动态生成的参数表单与「原始 HEX」标签页；右侧电机位置/速度/电流/使能状态与最近应答；底部收发记录（全部/TX/RX 筛选、搜索、暂停显示、清空、逐行复制）。
- 指令目录覆盖 `X42sProtocol.h` 中全部对外指令：使能/失能、立即停止、同步触发、力矩（F5/C5）、速度（F6/C6）、直通位置（FB/CB）、梯形位置（FD/CD）、14 个 `readSysParams` 读取项、控制模式、实时位置反馈间隔、当前位置清零、堵转保护复位、原点置零/参数配置/触发回零/中断，另有自定义帧构造。
- 常规试动：相对角度、方向、速度、加减速、电流上限、预测目标与预计时长，复用同一条 CD 组帧与同一份电机模拟状态。
- 发送预览：逻辑指令字节 + 按 `sendCommand` 规则拆分出的每个扩展帧（Packet 序号、ID、DLC、数据、逐字节注释），支持复制并弹出提示。

## 模拟规则与限制

1. **不访问硬件**：没有真实 CAN 收发，没有固件 HTTP 调用，所有 RX、ACK 与位置反馈都由本地仿真生成，界面中标注为「模拟数据」。
2. **ACK 不等于到位**：`0x02` 只表示固件收到指令，`0x9F` 只是完成回执；界面明确提示「不代表运动完成」。
3. **只模拟源码定义过的行为**：
   - 位置指令仅在运动模式为 `2`（MotionCore `kMotionModeRelativeToCurrent`）时按梯形公式模拟有界运动，仿真窗口 0.3–10 秒；其它模式字节只发送帧并提示「未建模」。
   - 直通位置的行程单位（clk）在源码中没有定义，因此只发送帧，不模拟位移与完成回执。
   - 速度模式为连续运动，最多模拟 10 秒后自动停转；力矩指令只更新电流读数，不建模力学。
   - 读取指令中只有 `0x27 / 0x35 / 0x36 / 0x3A` 有源码定义的应答布局，其余只发送 TX 帧，不构造数据帧、不回执。
   - 回零、控制模式、堵转复位、反馈间隔等网页未接入的指令只发送真实帧并给出模拟说明，不会伪造回零或参数生效。
4. **停止即作废**：会让电机状态发生变化的指令（使能/失能、停止、运动、力矩、同步触发、位置清零）都会递增该地址的纪元号，已排队的定时任务与进行中的运动全部作废，速度与电流读数同步清零；只读查询不会打断正在进行的运动。
5. **切换电机即收尾**：切换 CAN ID 会作废上一台电机的定时任务与运动，把它的速度与电流清零、保留最终位置，避免迟到的完成记录落到别的电机上。
6. **按地址隔离**：每个电机地址维护独立的模拟状态；切换 CAN ID 不会把状态搬过去，只会提示该地址是已有状态还是从零开始。
7. **不做静默截断**：整数字段必须落在源码给出的范围内，越界、非整数、NaN/Infinity 直接报错并禁用发送（固件 `constrainCurrentMa` 的截断行为在界面中被改为显式校验）。
8. **原始 HEX 编辑的约束**：首字节必须等于顶部 CAN ID；功能码已知但常量字节或长度不匹配时直接拒绝；只有功能码未知的自定义帧才放行为「只发 TX、无解码器」。
9. **同步选项**：`sync = 1`（含力矩）只把指令放入模拟队列，需发送 FF（同步触发）才按顺序执行；执行前会重新检查使能状态，失能会清空队列。
10. 常规试动的角度是正数幅值（0.1..3600°），方向完全由方向字段决定；规划器先校验原始数值范围再取整，与 MotionCore 的 `buildMovePlan` 一致。
11. 收发记录上限 400 条，超出后丢弃最早记录；「暂停显示」只冻结显示，不丢弃底层记录。
