# 闭环最大相电流 0x45 接入（2026-09-22）

入口：指令实验室 → 配置与维护 → 闭环最大相电流，支持搜索 45 / 闭环最大相电流。先关闭使能并等待静止反馈，再写入参数。是否掉电保存由表单选择，默认不保存。120 mA 为编辑示例，不是设备当前设置。

依据手册 V1.0.5 p82：`A 45 66 save current_u16 6B`，电流范围 0..5000 mA，额外遵循已保存的板端电流参数限制。不保存120mA示例 `01 45 66 00 00 78 6B`，保存100mA示例 `01 45 66 01 00 64 6B`。

这是所选电机的全局闭环相电流限制，不能当成回零专属电流，也不能与4C碰撞检测阈值绑定。碰撞阈值及转速、持续时间仍共同决定碰撞回零。未实现回零临时覆盖及恢复。

实现通过既有 Configure 路径发送，运动中、使能中或缺静止反馈时拒绝写入。新增布局/数值校验、网页控件、限制未加载时的发送拦截，以及实际45应答的只读解码。HTTP202仍仅表示入队；没有伪造参数读回值。

验证：npm test 60项通过；WSL Motion完整原生回归和固件构建通过；真实X42sProtocol.cpp在fake TWAI上验证45单包ID0x100、DLC6及字节；Playwright qa-current-limit 6项通过（mock API），含120/100mA、保存选项、各提交一次、无加载自动发送及限制缺失拦截。已查看1513×1039截图。

Claude Code运行模型deepseek-flash[1m]，服务api.deepseek.com；沿用会话4ae90a73-745c-456e-9347-4918ab551acf。Codex负责独立验证及真实驱动字节测试。

交付包：out/releases/motion-current-limit-20260922，含SHA256SUMS.json。检查firmware.bin包含当前motion/data/index.html完整字节。未烧录、未发送真实电机参数或执行运动；此包同时包含上一轮FB/CB直通位置支持。工作区其它改动保留，未提交。
