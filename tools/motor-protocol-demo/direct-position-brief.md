# Implement supervised FB/CB direct position

User says direct position is still not connected. Implement it now, not a plan. You own device-controller/include/ProtocolGate.h, device-controller/src/MotorControl.h/.cpp, tests/test_motor_control.cpp, necessary tests/fakes/fake_x42s.cpp, and tools/motor-protocol-demo/src/device-api.js, tests/device-limits.test.mjs, qa-direct-position.mjs. Read AGENTS.md in demo first. Other work exists; preserve all changes. Do not flash, commit, edit unrelated WiFi/queue/brain, or run shell (file-only tools); Codex builds/tests. Write a brief completion report direct-position-implementation.md.

Source contract: docs/ZDT_X42S/ZDT_X42S_Agent精简参考.md and full same-folder V1.0.5 transcription pp54-55 and p71. FB length12: addr FB dir speedBE16 angleBE32 mode sync 6B; CB length14 adds currentBE16 after sync before6B. Fields indexes speed3, angle5, mode9, sync10, current11. dir0/1; mode0 prior-input-relative,1 absolute signed target,2 actual-position-relative. Immediate sync0 supported; sync1 remains explicit unsupported (raw queue available). Preserve original FB/CB bytes and opcode; do not translate to CD or invent acceleration.

All three immediate modes should be usable. For mode0 use fresh actual driver target read0x34: CAN response [34 sign magnitudeBE32 6B] length7, sign0/1, in tenths degrees for X; do NOT substitute actual position or blindly trust last locally sent target. Add bounded fresh target polling without degrading existing position/velocity query cadence. Invalidate target sample after device-controller/config/raw/home etc as appropriate and reject missing/stale target honestly until refreshed. Use int64 arithmetic for signed magnitude and targets, verify target fits existing int32 feedback capability without overflow. Travel policy should check actual distance from current position to resolved target, not absolute coordinate magnitude. Mode0 also bounds input displacement. Mode1 zero target is valid. Zero displacement can be a no-op supervised by actual feedback; nonzero travel requires nonzero speed. For CB accept documented current0..5000 subject configured max, no arbitrary100mA floor (other existing paths unchanged). FB cannot impose a per-command current limit; make this clear in user note.

Use configurable DebugLimits speed/travel/duration; direct device-controller uses no artificial accel, estimate at commanded speed if useful, supervise with configured maxMoveDurationMs deadline (not invented acceleration/experiment timer). Require confirmed enable, fresh stationary position/velocity, no other owner, as existing move. Reuse MoveJob with opcode field default CD so ACK of other opcode cannot complete new job. Accept only matched valid FB/CB ACK;02 never physical completion, fresh target+stationary pairs required. Wrong opcode ACK must not set ackSeen. Failure/timeout/partial TX -> existing stop+fault behavior. Stop/disable cancel; no normal auto-disable. HTTP and UART busy gates already use job active.

Independent example: motor2,FB,CW,30RPM,90deg,mode2 => 02 FB 00 01 2C 00 00 03 84 02 00 6B. CB adds03 20 before6B for800mA. Current position100tenths +900 ->1000tenths; absolute CCW90deg=>-900; prior target2000 +900=>2900, not current+900.

Frontend: remove FB/CB preview-only; mirror exact layout/sync/limits checks, mark coordinate-dependent checks board-resolved, handle errors clearly via existing error map. Ensure DeviceApp command send flow actually enables FB/CB with loaded limits (inspect no stale hardcoded lists). No mobile work. Add meaningful native tests modes0/1/2, missing/stale target, zero absolute, matching ACK and fresh completion, wrong opcode, limits/overflow, TX fail/cancel/timeouts; JS tests encoding support and bounds. Add a desktop Playwright mock API flow demonstrating FB and CB send exact bytes once and sync remains gated, follow qa-device conventions.
