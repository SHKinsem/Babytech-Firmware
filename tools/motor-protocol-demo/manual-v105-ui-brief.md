# Checkpoint 2: integrate the reviewed manual model into the device webpage

Codex independent checkpoint tests: 24/26 passed. Two findings: decoder incorrectly accepts sign02 on39 (and other signed opcodes); fix sign rejection0/1 before any decode. Old read-count test14 now21 will be updated by Codex (expected catalog expansion). Additional review corrections: homingProgress flags0x0c currently falls through to false '0x00'; explicitly report simultaneous running+failure without claiming success. Fix comment '11-bit ID <=FFFF' to extended identifier with address in bits8..15. Remove ungrounded claim in 36 reply text that input0.01degree setting changes feedback conversion: p72 always defines returned Xposition /10; p80 setting is input scaling. Keep input compatibility note. Simplify long decoder prose to user-meaningful values and short annotations, especially3A/3B/3C renderBits use newline-delimited short labels, not paragraphs on each bit; retain caution notes. Do these decoder fixes FIRST, then integrate UI.

Implement now. Own only src/DeviceApp.jsx, src/device-api.js, src/device.css, src/components/CommandPanel.jsx, and optionally src/manual-reference.js for review corrections below. Other edits already exist (ScaleWorkbench/Wi-Fi): preserve them. Codex owns protocol.js review, tests, backend and docs. File-only tools, Codex runs tests/build/Playwright. Desktop1513x1039 and1280x800, no mobile, no hardware, no network and no git. Existing white/indigo three-column layout remains.

Integrate decodeCanReply into /api/trace incoming rows. Store each decoded object as row.decoded, append human-readable text in row.note. Only valid actual RX frames accepted by pure decoder; no fabricated data. Reboot detection currently generation increments: clear old records on reboot so old diagnostics do not survive. Keep seq coverage warning. Raw frame remains visible.

Right DeviceFeedback receives current target address, selected opcode and records. Add a compact '查询结果' section near metrics when in lab, showing latest decoded record for that selected opcode/address with local receive timestamp and '最近收到 · 历史记录，非实时保证' label. When none show '尚未收到此查询的有效回包'. Do not show another motor's data. When disconnected clear/hide result. Latest record may be stale: label clearly, never use it to drive permission/position/enable state. Ordinary telemetry metrics continue using backend status. This makes new reads useful rather than only a catalog. For 3A/3B/3C use multiline compact text (white-space pre-line) within scrollable feedback panel. Traces may carry long descriptions use existing clipped note/title.

Show concise visible X protocol compatibility note ('X 固件 · 默认 0.1°位置输入 · 固定校验 6B') and manual V1.0.5 source in feedback expandable details. Explain Emm default firmware uses different frames; this page does not auto detect nor convert;0.01degree configured input is not supported. Keep obvious label visible but long explanation collapsed. Distinguish manual documented abilities from currently permitted backend operations; keep 120RPM/5sec and unsupported mode gates. No F1/FC/sync enablement.

Update supportReason read whitelist to add1A26 32 34 39 3C3D (Codex updates backend exact same list). Preserve all current protections.

cleanItem currently removes note; display new manual descriptions without outdated simulator promises. Could keep original note removal but manual compatibility handled separately. CommandPanel many-option enum (homing6,motion3) must wrap or select so no horizontal overflow; scope small CSS appropriately and retain radio labels used by tests. Show source-backed field help already certaintyLabel supports manual. If per-command meaning currently visible summary ensure it remains.

Review decoder: reject invalid sign or bytes, remote/std/packet1/unknown IDs. Motor bit7 should say power-loss flag0 resetdefault, not powerfailureactive. Temperature39 sign0negative1positive; combined bulk43 different sign not decoded. ACK12/22 only interpret as homing noaction when opcode9A, otherwise raw/unknown. Do not decode option1A guesses. Read payload02 never ACK.

Finish concise report with owned paths, no test claims. No rewrites of other tabs.
