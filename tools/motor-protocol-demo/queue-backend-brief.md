# Stage 2 board queue implementation

Before queue implementation: address independent review findings in queue-review-notes.md (home stale proof + stop pending desire). Ownership also tests/test_motor_control.cpp for regression fixes. Initial stage1 test compile missing private kFrameHome was fixed by Codex with independent test literal; do not undo. Current homeOutcome string is no_motion and flags field homeOrg; keep and document names for frontend integration. Add homeFlags alias only if useful, do not arbitrarily rename contracts.

Latest native test run:866checks3failures only test_home_param_bounds_and_power_on_report at1141 configured.code==202,1142messagequeued,1145count4C==3. Investigate fixture freshfeedback/nodeOfInterest beforechangingimplementation. All other31scenarios passed. Fix this first together with review findings, then queue.

Implement through Claude Code/DeepSeek. Read tools/motor-protocol-demo/queue-contract.md for authoritative full API and DSL. Ownership: new device-controller/src/CommandQueue.h/.cpp and/or device-controller/include/QueueProgram.h; device-controller/src/main.cpp (only queue/profile/routing integration); device-controller/src/MotorControl.h/.cpp only minimal direct transport API + queue support, preserve stage1; device-controller/lib/XMotor/src/X42sProtocol.h/.cpp only new exact raw send/logical public API; tests/fakes/fake_x42s.cpp if new methods need stubs; new tests/test_command_queue.cpp; tools/test_motion.py append queue suite preserving existing. No frontend/other docs/BoardMotion edits. Other agents/tasks editing repository, don't revert them. Codex tests/builds; file tools only.

Queue contract file is binding. Nonblocking RAM executor, fixed64steps, repeat1..1000, validation full program BEFORE sending any CAN, error source line, immutable plan until stopped/done. Strict numeric parse signed decimal finite, int exact, no partial parse/trailing garbage, total text bound8192. Prefer Arduino-independent parser or portable class native-testable with existing fake Arduino String (limited API: c_str/length/+= constructors). Avoid ArduinoJson dependency; HTTP text DSL form is enough. Memory fixed bounded. RotationDistance per-ID stored by main.cpp NVS namespace motor-distance keys d1..d255 float/double; GET missing null, POST0clear orfinite positive1e-6..1e6, explicit NVS save success only updates RAM. Queue resolves mm at validation from current per-ID values, never defaults. rev360deg. Same DebugLimits used structured actions. Clamp nothing. Signed torque/rpm denotes direction.

Readable step execution: enable waits actual ACK and fresh stationary before advancing, move calls existing motor.move and checks new moveOutcome; home calls stage1home and checks newhomeOutcome. 12/22 NoMotion reported distinctly; queue can continue if fresh stationary confirmed (don't claim homing occurred). Timed C5/C6 validate duration <= nonzero experiment policy, send once, timer stops then waits fresh stationary. Zero speed/current disallow as timed action (use stop). No implicit enable. Watch nexttarget then wait up to3000ms for fresh feedback BEFORE dispatch when target changes; must not consume stale previousstepACK/outcome or retry rejected dispatch. Stop waits stopped. General failure stops all and keeps first error; timeout must not silently advance. Bus fault or unexpected cancellation stops remaining. Queue status progress meaningful,runIdincrementstart, rawflag if containsraw. One action can start per poll at most, raw steps >=2ms apart.

Raw hex transmits address/opcode/body exactly using existing fragmentation repeatedopcode (n3..30), skips whitelist intentionally. raw CAN supports11bit/29bit actualID,data0..8 arbitrary, no6B rewrite, standard/extended, oneTWAI submission and actualtrace. Both transport entry points require busready and no active supervised operation; do not silentlystop/enable; invalidate obsolete software confirmation on rawmutation so laterstructuredneedsenable. Do not alter existing correct fragmentation. New rawdriver method validatesID,length,usesboundedtwai_transmit trace, single-shot preferred. Unknownraw outcomes only sent. Queue raw completion never claims physicaldone. Cancel raw queue should broadcast9C48 andFE98006B since rawhome may not be tracked. Returntxfailure immediately; no appretry. If framepartfails don'tsend laterparts in rawlogical path.

main.cpp integrate globalqueue(motor), queue.poll aftermotor.poll, GET/POST endpoints percontract; all mutation API handlers rejectqueuebusy exceptstop/disable/abort which CANCEL queue BEFORE controller action. readcommandsallowed. WiFi callbackbusyincludesqueue.active. limits/profile/configbusyincludesqueue. HTTPstart reject endpoint.busy/wifi.busy/controller.operationBusy. UART endpoint must seequeueactivevia boardMotion.setRadioBusy(wifiBusy||queue.active); UART STOP must cancelqueue before dispatch (inspect BoardProtocolV2 opcode contract and frame fields). No parallel mechanical ownership acrossUART/HTTPqueue. Already-authorized stop should remain functional evenifqueuesoftwarefailed. No new commands on boot. queueclear on reboot. GET doesnotenable.

Tests native whole program atomicvalidation,source lines,perIDmm+revconvert,strictnumeric/rawDLC-IDlimits,allstructuredstep progressionwithfakeCAN,enableACKnot stale,homeidle00notadvance,cancel preventsnextstep,rawfinishsentnotdone,repeatsbounded,timeout/faultfailfast,policy limits, stopretainenable. Add sources to native runner; fake impl also supports new rawdriver method and trace. Do not claimtests run. Return API details and any mismatch with queue-contract.
