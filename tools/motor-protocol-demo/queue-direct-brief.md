# User correction: make the queue a simple command sender

Latest user: “校验这么多东西干什么，这就是一个类似gcode的功能…别搞太多花里胡哨的功能…兜底的事给我自己判断”. This EXPLICIT instruction overrides previous AGENTS supervised-queue requirements. Implement direct execution now, not a new selectable safety mode or another permission flow. Retain requested logs and clear-state button; no extra recovery UI. Codex handles final QA/build, file tools only.

You are not alone; preserve all current edits, including Codex's latest reset/control blocker implementation, Endpoint.cancelPending, tests, logging fixes. Own CommandQueue.cpp/.h, minimal additions MotorControl.cpp/.h for queue-only direct transport (DO NOT change existing manual/lab supervisor), main.cpp queue start handler only, queue-program.js, QueuePanel.jsx, relevant queue status wording device-api.js/DeviceApp.jsx, queue/native tests and QA. Do not overwrite logs/reset code. Read current files, not remembered versions.

## Semantics: exact, small and direct

- Queue translates readable DSL to CAN and sends in order. No waits for enable ACK, stop/position/current feedback, position reached, homing completion or configuration readback. No stale-feedback/fault/enable/standstill checks. Historical motor state cannot reject start or a step. No board DebugLimits policy checks or max-duration policies for queue. No requirement /api/limits loaded in frontend to start/validatequeue. No inferred enable lines. Defaults are just encoded values.
- Only user-written `wait MS` waits. Existing torque/velocity explicit DURATION_MS is user requested timing: preserve compatibility by sending command, waiting that duration, then sending FE, advancing immediately without stop feedback. Additionally allow short `torque ID SIGNED_MA` and `velocity ID SIGNED_RPM` with NO duration, sending only and continuing. Don't add implicit stop on program end, fault, missing feedback or timeout. Explicit stop/disable, cancel/全部停止 and clear-state still do what user requested. On actual TX failure terminate queue with source line/error; do not broadcast extra stops automatically or retry.
- Move/home lines report sent, not mechanical completed. All queue done text “发送结束”, avoid claim motor reached. Consecutive move/home lines CAN be sent while prior motor physically working: user uses wait if wanted. Explain in one compact help line, not warning wall. Driver ACKs/errors are diagnostic only, don't release/block/killqueue.
- Start takes ownership from stale software supervisor by clearing MotorControl tracking (existing clearControlState is software only), AFTER full parse success. No implicit CAN stop/enable on start. Busy queue still rejects concurrent queue start to preserve program order. Actual unavailable CAN still reports sendfailure, not pretend success. main handleQueueStart remove endpoint/motor/wifi supervision gates that block valid direct program; handle UART ownership cleanly: cancelPending/sendFrame only AFTER valid successful queue.start, so invalid program has no side effects. During running queue existing guard prevents manual/UART writes from interleaving. Do not change unrelated Wifi settings.
- New queue-only MotorControl direct send methods (or explicit flags) use can_.sendRawLogical / sendRawFrame with only buffer/ID/DLC and CAN readiness checks; bypass operationBusy and do NOT start 4C config transaction. Existing rawLogical/manual command paths keep their prior semantics. No noteRawTransmission needed per frame after start clears tracking. Existing trace records real wire frames. Poll queries okay; don't invoke supervisor command()/move()/home()/enable()/stop() from direct queue steps since they resurrect hidden tracking.
- Wire layouts must come from existing protocol sources/tests. Explicit enable/disable F3; move CD relative mode2; home9A with usermode sync0; torque C5; velocity C6; stopFE; hex/can untouched. Big endian and CAN repeat opcode fragmentation preserved. Units: move deg/rev/mm by known per-ID rotationDistance only (missing conversion still reports cannot encode, don't invent value).
- Retain only encoding/parser/resource bounds: ID1..255 (rawcan rules), finite numeric grammar, valid enum/uint16/uint32 fields per documented protocol, hex length/checksum compatibility, max64steps/8192text/repeat1..1000. No prior enabled-order validation, planner duration/distance bounds, 100mA arbitrary floor or acceleration minimum1 when protocol allows0. Use X wire protocol speed0..3000RPM/current0..5000mA, acceleration0..65535 etc based actual manual. Existing signed distance int32 representation can retain explicit implementation bound if widening costly, but no saved DebugLimits maxangle. Parse before integer narrowing; don't wrap overflow. Config4C does NOT force enableoff/readback barrier in queue. Manual/lab config supervision unaffected.
- Remove dead supervisor phases/methods from queue, keep a small idle/timer dispatch state. No duplicate mode, no unnecessary framework.

## UI

- QueuePanel validation independent of live enabled/fault/feedback and loadedlimits; only connected API / concurrent queue submit tracking and syntax gates. Action builder must not default to adding enable; remove hidden enable injections. Keep explicit enable verb and optional user-choice insertion if alreadyexisting checkbox, defaultfalse.
- Simplify help to “按顺序发送；等待请写 wait；结束只表示发送完毕”. Timed old torque/velocity definitions honestly state userduration includesstop; short form noautostop. Keep rotation distance selection and drafts/log/reset.
- Actualqueueerror codes showline/transportreason; UI cannot call missing-feedback/fault_active reasons operationlocks for queue. No auto retries.

## Tests

- Update old queue-supervision assertions to new user-requested contract, don't delete parser/overflow/transport tests. Real MotorControl + fake hardware must execute enable/move/home/move without a SINGLE RX and despite preexisting fault or pendingstop; no unsolicited FE/F3. Wait ensures timestampordered frames; cancellation prevents latersteps and explicitlystops; timedlegacy adds FE onlyafterwrittenduration; shorttorque/velocity noautostop. 4Chex followedbyhome sendsbothwithout22readbackbarrier. Tight savedlimits don'treject validprotocolqueue. Syntaxfailuremustnotclearoldstate. TXfailurestopsqueueonly(noextraCAN).
- Update tests/test_config_recovery.cpp expectations ONLY queueparts; retain laboratory 4C readback verification coverage through motor.command if applicable. Update test_board_motion etc only if new queue behavior demands it, not weaken unchanged manual tests. Actual transport test bytes remain exact.
- Frontend puretests + qa-queue direct browsercheck showing missinglimits and driverdisabled/fault still permitsvalidqueue; noimplicitenable; programsubmitonePOST; oldmanualgates preserved.

Finish compactly. Summarize contract changes. Codex runs tests/build; noflash/commits.
