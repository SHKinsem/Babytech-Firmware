QA review from coordinator. Keep all existing files and fix the concrete issues below. The first coding pass has been built and browser-tested by me. Dependencies including @phosphor-icons/react ARE installed. Use the Windows PowerShell tool, not Bash (npm absent in its Linux shell). Run npm test and npm run build using PowerShell if allowed; I will handle browser screenshots and UI tests. Do not start a server or run QA scripts. You are not alone: coordinator owns qa-*.mjs and qa/; do not edit those.

1. Browser currently empty with ReferenceError TENTHS_PER_RPM is not defined from planManualMove in simulation.js. Fix import. Add planner tests to cover defaults and directions.
2. Tests have incorrect expected values: originModifyParams logical bytes length is 20, not 19 (check C++), DLCs [8,8,5]. For expectedDurationMs(1800,600,60,60), triangular motion takes ceil(sqrt(2)*1000)=1415ms, not600ms. Fix math commentary and both related tests; do not change correct encoder/duration to satisfy wrong tests.
3. Manual direction control currently ignored: planManualMove derives dir from angle sign only; UI has BOTH signed angle and direction. Make angle a positive magnitude 0.1..3600, direction solely input.dir (0 or1); enforce finite raw ranges BEFORE rounding as MotionCore does. Update helper copy, defaults angle90 current800. Position target uses signed deltaTenths; no duplicated direction semantics.
4. Raw known opcode params can bypass validation: model matched raw command then returns ok:true even rebuilt.ok=false. Propagate rebuilt errors. Known opcode with wrong constant or missing bytes must be rejected (not silently treated as unknown custom success). Require raw command address matches top address so simulation address and TX address cannot diverge. Unknown opcodes allow TX only, no fabricated ACK. Enforce maximum 255 logical bytes in custom encoder too.
5. 全部停止 currently sends addressed FE to current address rather than actual broadcast. Log actual extended CAN ID0, DATA FE 98 00 6B; cancel all tasks/queues; do not fake a single-motor ACK as broadcast acknowledgement. Keep normal address validation1..255 unchanged and handle broadcast through this explicit path.
6. Changing motor must cancel previous motor pending tasks/motion per brief, set its speed/current to0 and preserve its final position. No delayed completion after change. Invalid address should gate manual tab as well as command lab. A read/query should not cancel ongoing motion and leave stale nonzero speed. Mutating replacement/stop should consistently clear motion speed/current. Sync torque must queue too, not apply immediately. Disable clears queue. Recheck enabled when running queued motion.
7. Position modes other than known mode2 and passthrough unknown units should expose frames but not fabricate relative motion/completion. Return an honest simulation-not-modelled note. Only simulate known relative trapezoid mode2. Unknown read response layouts must not fabricate protocol ACKs unsupported by source; TX-only note is fine. Keep verified 0x27/35/36/3A feedback simulation.
8. Segmented component Number(value)===option.value fails variants whose keys are strings. Compare appropriately for string and numeric options.
9. Icons file uses handcrafted CSS icons despite installed Phosphor package. Replace glyphs with @phosphor-icons/react equivalents (MagnifyingGlass,CaretRight/Down,Info,Circle), remove unneeded CSS drawings. Preserve layout.
10. Keep UI faithful and edited: field long implementation hints should be disclosure/help title, not long paragraphs under every control. Original selected F3 screen should be compact and readable, actions visible. Do not crowd with developer terminology. Update README to reflect capabilities and simulation limits accurately.

Implement fixes via Read/Edit/Write tools, no shell permission workaround, no new dependencies. End when npm test/build pass or report actual execution blocker. Main coding work must remain yours via Claude Code. Avoid lengthy planning; act on this bounded correction list.
