# Independent stage2 review observations (work in progress)

Codex first stage2 native run: controller892checksPASS (including3newreviewregressions), UART+controllerPASS, queue compilationFAIL test_command_queue.cpp poll() missingnow arg at66/398/468; makeAck(idle,0x9A,0x02) at460 passesQueueRig instead ofaddress. Fix obviousfixtures before next run. No productioncompilererrors reported inthis snapshot.

Parser blockers: Tokens.length isuint8; tokenize casts withoutchecking i-start<=255, so257char token truncatesto1 and mayacceptgarbage. Rejectoversizetoken beforecast orusesize_t. parseHexId permits10hexcharacterswithout0x then32bitshiftwrap -> accepts100000000 asID0. Explicitmax8digits AFTER optionalprefix and checkoverflowbefore shift. Tests 257char numeric,9/10hex digitID,embeddedNUL allreject withoutsending.

Additional contract blockers found08:35: parseMoveStep token count max7 incorrectlyrejects fullbuilderline `move 1 90 deg 30 60 60 800` (8tokens) despitevalidcontract. Allow8whenunitpresent,validateoptionalcount correctly. CurrentMa parsedlongthen narroweduint16 BEFOREplanner validation: `move 1 90 30 60 60 66336` wraps800 andpasses; validate100..limits.maxCurrentMa beforecast. Addfull8tokenfixtureandwrappingoverflowfixture. Currentnativefixturesmaynotcoverthese.

Read these after initial implementation before final verification. Ownership same as stage2.

- CommandQueue::start local QueueProgram parsed (~6KB or more) + parseQueueProgram program=QueueProgram{} temporary risks ESP32 loop stack (8KB default). Put validation scratch in class/BSS or checked heap, avoid any wholeprogram localtemporary in parser/reset; measure sizeof and firmware stack usage. Native stack ample hides this.
- waitHome NoMotion branch currently has no timeout if feedback stays unavailable after outcome. Must bound phase with configuredmovebudget/freshness and requeststop, not stayrunningforever.
- waitEnable currently stationary(snapshot) may use pre-F3 positions; require snapshot.positionAge andvelocityAge < elapsed since dispatch, !enablePending, !stopPending fordisable. NoMotion must similarly require post9A/post-noMotion feedback, not oldstationary. Tests inject oldonlyack and ensure nextstepnotrun.
- beginStep lastTarget optimization dispatches immediately after homeDone invalidatesposvel, so consecutive home1;move1 will rejectfeedback instead of waitingfornewpair. All supervisedmotion steps should acquirefreshstationary beforedispatch (bounded), not just targetchanged. stop/disable must dispatchpromptlywithoutrequiringpreexistingfeedback; they must stop eveniftargetquiet. Rawthenstructured sameID must resetlastTarget/obtainnewfeedback.
- parseTorqueStep maxRPM currently integer while frontend/doc maxRPM supports0.1 resolution: align todecimal RPM andvalidate protocolpolicy thenroundtenths. Signednumeric grammar mustmatchfrontend (-optional,noplus,noexponent). ID integers shouldn'taccept1.0 whenfrontenddoesn't.
- Raw queue Done currently message done; use frames_submitted or explicit 'raw_frames_submitted' whenrawpresent so UI doesn'tassertmechanicaldone. Keep raw flag trueforwholeprogram. UI should mapmessage accordingly.
- UART STOP must cancelqueue only after validating actual request/shape (don'tletmalformedframes mutatehardware). Prefer stop hook or validSTOPrequest withlength/target/flags plusCRCparser, inspectendpoint rules. QueueLockvia setRadioBusy only blocks ifBackendbusychecked; verify EXEC/ENABLE againstqueue state.
- Actual driver raw send needs test beyond fake because fake reimplementing fragmentation couldsharebug. Independently expectedframesinqueue-review-notes.md.
- main handleStop and handleEnable(false) call queue.cancel unconditionally, which broadcasts stop to ALL motors evenwhenqueueinactive and user targetedjustone. Cancelactivequeuefirstonly; otherwisepreserveoriginalsingle-targetstop/disable semantics. Explicit /queue/cancel and /stop-all shouldbroadcast always. handleCommand mustallow Read whilequeueactive (current !stopLike rejection incorrectlyblocksqueries). Don'tcallglobalqueuecanceloninvalid stopLikeframes (validatekind and enableexactbytes).
- clearRotationMm currently ignores prefs.remove return and alwaysreports success. Check existingkey withisKey thenremoveactualreturn; missingkey is alreadycleared. loadRotationDistances shouldisKey/getBytesLength beforegetBytes toavoid255 missing-key errorlogs. GETprecision shouldn't turn allowed1e-6positiveinto0.
- mainhandleStatus currentlywatch(selectedUIid) every300ms; afterenableACKqueuewaitstationaryforanotherid couldlosequerytarget. Whilequeueactive GETstatusmustnotchangepollingtarget; keepcurrentstructuredqueueidwatchedacrossallphases. Readcommandforotherid may sendread but shouldn'tstealqueuefeedbacktarget.
- Codex added tests/can_fakes, tests/test_raw_can.cpp and tools/test_raw_can.py (ownershipCodex, don'tedit) independently compilingactualX42sProtocol.cpp. PASSactualCAN fragmentation, exactIDbytes, single-shot,partialTXfailure,boundsandtrace. Addtest_raw_can.py invocation to native runner onlyafteryours done (Codex can integrate). Rawlogical currently omits existing2ms interpacketgap; preserve boundedgap forconsistentdriverbehavior.
